// This TypeScript file was moved to the frontend `src/shared/services/searchKnowledgeBase.ts`.
// It's intentionally left as a minimal placeholder to prevent accidental server-side usage.
// If you need server-side search, implement it in Python under `backend/` and expose it via FastAPI.

export {};
